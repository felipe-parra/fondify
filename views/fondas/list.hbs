<div class="container">
  <div class="section">
    <div class="row">
      <div class="s12 m12">
        <h2>Si nos permites saber tu ubicación podemos darte las fondas mas cercanas a tí. 😉</h2>
      </div>
    </div>
    <div class="row">
      {{#each fondas as |fonda|}}
      <div class="s12 m4">
        <div class="card">
          <div class="card-image">
            <img src="{{fonda.img}}">
            <span class="card-title">{{fonda.name}}</span>
            <a class="btn-floating halfway-fab waves-effect waves-light orange" href="/{{fonda._id}}/fonda"><i class="material-icons">add</i></a>
          </div>
          <div class="card-content">
            <p>{{fonda.description}}</p>
          </div>
        </div>
      </div>
      {{/each}}
      {{#each fondas as |fonda|}}
      <div class="s12 m4">
        <div class="card-grid">
          <wired-card elevation="3">
            <h4>{{fonda.name}}</h4>
            <div class="card-image">
              <img src="{{fonda.img}}" alt="{{fonda.title}}">
              <wired-icon-button class="btn-floating halfway-fab orange" href="/{{fonda._id}}/fonda">favorite</wired-icon-button>
            </div>
            <p>{{fonda.description}}</p>
          </wired-card>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>